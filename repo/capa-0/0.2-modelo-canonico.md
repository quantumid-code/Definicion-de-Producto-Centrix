# Centrix · 0.2 Modelo canónico de dominio

**Capa 0 · Fundamentos** · Versión 0.2 · 9 de septiembre de 2026 · Interno confidencial

Depende de: 0.1 Glosario canónico (v0.2) · 0.3 Registro de decisiones · 0.4 Reglas irrevocables

> **Regenerado.** Sustituye a la versión 0.1 del 16 de agosto de 2026. Trazabilidad en
> `CONTROL-CAMBIOS.md`, fila 0.2.
>
> **Insumos que motivaron la regeneración:** Prosa · Estándar Host POS Adquirente **8.12.0**
> (19/05/2026) · Prosa · Especificación Online para Servicios de Agregadores **3.8.0**
> (24/02/2026) · Prosa · Especificación Estándar de Tokens **0.17.0** · Prosa · Mejores
> Prácticas de Códigos de Respuesta **1.4.0** (27/04/2026) · E-Global · Anexo A **9.0**.
>
> **Qué cambió:** entidad nueva en el apartado 4 (identificador de facilitador de pagos);
> máquina de estados nueva en el apartado 4 (estado criptográfico de terminal); el escenario
> de agregación pasa a ser atributo de la afiliación; el ciclo del dinero incorpora la
> compensación por fichero; tres invariantes nuevos (I16 a I18); resultado canónico gana
> categoría de rechazo.
>
> **Decisiones provisionales aplicadas:** N-3, N-4, N-5, N-6, N-7.
>
> **Advertencia de referencias.** Las citas a D40, D44, D45 y D19 provienen de la versión
> anterior y **no son verificables** contra la copia de `0.3` presente en el repositorio, que
> define hasta D22. Se conservan sin alterar para no perder la intención original; se
> validan cuando se reemplace `0.3`.

---

## 0. Qué es y qué no es este documento

Define **qué entidades existen, en qué estados pueden estar, qué transiciones son válidas y qué
invariantes no pueden romperse nunca**. De aquí se derivan los esquemas JSON, el DDL y los contratos,
en ese orden y no en otro.

**No define**: campos de trama, formatos, longitudes, códigos de tercero ni nombres de columna. Eso
es la especificación de dialecto y la tabla de mapeo.

**Regla de lectura.** Si una transición no está en este documento, no es válida. La ausencia es
prohibición, no omisión.

---

## 1. Los cinco principios de diseño

### P1 · Canónico primero, adaptador después
El adaptador traduce hacia el canónico, nunca al revés. Regla irrevocable 8.

### P2 · Concepto de negocio, no artefacto de trama
Un plan de promoción a plazos entra aunque no exista en la especificación internacional. Un campo
privado de sesenta posiciones no entra aunque sea obligatorio en una cámara. Criterio de D22.

> **Matiz incorporado en esta versión.** Un valor de campo deja de ser artefacto de trama cuando
> determina economía o soberanía. El modo de acceso y el escenario de agregación viajan en campos del
> mensaje y **sí entran al canónico**, porque deciden qué tarifa se cobra y bajo qué identidad se
> construye el historial. Lo que no entra es el literal con que cada cámara los codifica.

### P3 · Declarado frente a observado
Donde hay un tercero autoritativo, se registran ambos por separado. La divergencia es información.
Aplica a afiliación, sub-afiliación, terminal, ruta y libro contable. Decisión D40.

### P4 · Toda instrucción hacia un tercero pasa por un estado intermedio
No existe la escritura local que cambia el estado de algo que vive en otro sistema. Se solicita, se
deja de operar, y se confirma. Decisión D44.

### P5 · Lo indeterminado no mueve dinero
Es el invariante que gobierna la relación entre el ciclo transaccional y el contable. Decisión D19.

---

## 2. Mapa de entidades

```
adquirente
   └── afiliación                        (marcas habilitadas, cuenta concentradora,
         │                                tasas por giro, ESCENARIO DE AGREGACIÓN)
         ├── identificador de facilitador  (uno por marca · otorgado por la marca)
         └── sub-afiliación ≡ comercio     (razón social · conocida por la cámara)
               └── sucursal                (nivel derivado · no conocida por la cámara)
                     └── terminal          (identidad dual + estado criptográfico)

intención de cobro
   ├── decisión de ruteo                 (afiliación elegida + ruta pretendida)
   ├── intento *                         (una ejecución contra una ruta)
   │     └── resultado                   (+ categoría de rechazo)
   ├── autorización
   ├── captura *                         (integrada o diferida a fichero)
   ├── devolución *
   └── autenticación

lote
   └── transacciones del punto de aceptación entre dos cortes

fichero de compensación
   └── transacciones de captura diferida pendientes de compensar

libro contable
   └── asiento *                         (partida doble · estado dual)

conciliación
   ├── movimiento conciliatorio          (hecho reportado por un tercero)
   └── partida de conciliación           (resultado del cotejo)
```

Cuatro niveles de negocio, dos identificadores de red. La sucursal es un nivel que Centrix conoce y la
cámara no. **El identificador de facilitador cuelga de la afiliación y no de la jerarquía comercial**,
porque su emisor es la marca y no el adquirente.

---

## 3. Ciclo transaccional

### 3.1 Intención de cobro · `payment_intent`

Objeto raíz. Expresa qué se quiere cobrar, a quién y por cuánto. Persiste a través de N intentos.

| Estado | Significado |
|---|---|
| `creada` | Registrada, sin intento emitido |
| `en_proceso` | Hay un intento en vuelo |
| `autorizada` | Un intento aprobó; fondos reservados |
| `capturada` | Capturada en su totalidad |
| `capturada_parcial` | Capturada por menos del importe autorizado |
| `rechazada` | Todos los intentos declinados y no quedan rutas |
| `anulada` | Anulada antes del cierre de lote |
| `devuelta` | Devuelta en su totalidad tras compensación |
| `devuelta_parcial` | Devuelta por parte del importe capturado |
| `expirada` | La autorización venció sin captura |
| `indeterminada` | Ningún intento aprobado y al menos uno sin resolver |

**Transiciones válidas**

| Origen | Evento | Destino | Guarda |
|---|---|---|---|
| `creada` | intento emitido | `en_proceso` | Existe decisión de ruteo persistida |
| `en_proceso` | intento aprobado | `autorizada` | No existe ya otro intento aprobado (I7) |
| `en_proceso` | intento declinado, quedan rutas | `en_proceso` | Categoría de rechazo admite reintento (I8, I18) |
| `en_proceso` | intento declinado, sin rutas | `rechazada` | — |
| `en_proceso` | intento indeterminado | `indeterminada` | — |
| `indeterminada` | respuesta tardía aprobatoria | `autorizada` | — |
| `indeterminada` | respuesta tardía declinatoria | `rechazada` | — |
| `indeterminada` | reverso confirmado | `rechazada` | — |
| `indeterminada` | reverso emitido por la cámara | `rechazada` | El reverso no lo originó Centrix (3.2) |
| `indeterminada` | resuelta por conciliación | `autorizada` o `rechazada` | Según lo que confirme el tercero |
| `autorizada` | captura total | `capturada` | Suma de capturas ≤ autorizado (I2) |
| `autorizada` | captura parcial | `capturada_parcial` | Idem |
| `capturada_parcial` | captura adicional | `capturada` o `capturada_parcial` | Idem |
| `autorizada` | vencimiento | `expirada` | Transcurrida la ventana de la marca |
| `autorizada` o `capturada*` | anulación pre-corte | `anulada` | Lote aún abierto |
| `capturada*` | devolución total | `devuelta` | Suma de devoluciones ≤ capturado (I3) |
| `capturada*` | devolución parcial | `devuelta_parcial` | Idem |

**Terminales:** `rechazada`, `anulada`, `devuelta`, `expirada`. Desde `devuelta_parcial` y
`capturada_parcial` se puede seguir operando.

> **Nota de diseño.** `venta` no es un estado ni una operación. Una venta directa es
> `autorizada` seguida inmediatamente de `capturada`, con dos asientos contables distintos.
>
> **Confirmación externa incorporada en esta versión.** La cámara lleva una bandera explícita que
> declara si el mensaje separa autorización de compensación o las integra secuencialmente. La
> separación no era una postura de diseño de Centrix: es una opción del protocolo. Ver 5.1b.

---

### 3.2 Intento · `attempt`

Una ejecución contra una ruta concreta. Aquí vive el estado indeterminado; la intención solo lo
refleja.

| Estado | Significado |
|---|---|
| `preparado` | Decisión de ruteo tomada, aún no enviado |
| `enviado` | En vuelo hacia el procesador |
| `aprobado` | Respuesta aprobatoria recibida |
| `declinado` | Respuesta declinatoria recibida |
| `indeterminado` | Sin respuesta dentro de la ventana |
| `reverso_solicitado` | Reverso emitido, sin confirmar |
| `revertido` | Reverso confirmado |
| `resuelto_tardio` | Llegó respuesta después del vencimiento del envío |
| `abandonado` | No resuelto por vía técnica; escalado a conciliación |

**Transiciones válidas**

| Origen | Evento | Destino |
|---|---|---|
| `preparado` | envío | `enviado` |
| `enviado` | respuesta aprobatoria | `aprobado` |
| `enviado` | respuesta declinatoria | `declinado` |
| `enviado` | vencimiento sin respuesta | `indeterminado` |
| `indeterminado` | respuesta tardía | `resuelto_tardio` |
| `indeterminado` | política decide revertir | `reverso_solicitado` |
| `indeterminado` | **reverso originado por la cámara** | `revertido` |
| `reverso_solicitado` | confirmación de reverso | `revertido` |
| `reverso_solicitado` | vencimiento del reverso | `abandonado` |
| `indeterminado` | vencimiento de la política | `abandonado` |
| `abandonado` | resuelto por conciliación | `aprobado` o `declinado` |

**Política de resolución del estado indeterminado.** Configurable por ruta, con estos ejes: ventana de
espera antes de declarar indeterminado, si se emite reverso automático o se espera, número y espaciado
de reintentos del reverso, y ventana antes de abandonar y escalar a conciliación.

> **El techo de la ventana no lo pone Centrix.** La cámara publica su propio límite de espera hacia el
> emisor, y ante respuesta tardía aprobatoria **emite el reverso por su cuenta**. La política de
> Centrix se configura por debajo de ese techo, nunca por encima, y el modelo admite reversos que
> Centrix no originó. El valor concreto es artefacto de dialecto y vive en la tabla de mapeo.

> **Por qué existe este estado.** La interfaz del procesador de Fase 0 no expone clave de
> idempotencia, no admite referencia del integrador en el cobro y no publica consulta de transacción.
> Si la respuesta se pierde, no hay forma técnica de saber si el cobro ocurrió. D19, D16.

---

### 3.3 Resultado y categoría de rechazo · `outcome`

Todo resultado declinado se clasifica en una **categoría de rechazo** que determina la elegibilidad de
reintento. La categoría es atributo **calculado por Centrix a partir del mapeo**, nunca recibido del
integrador ni decidido por él.

| Categoría | Reintento | Efecto en la intención |
|---|---|---|
| `definitivo` | Prohibido | La intención pasa a `rechazada` aunque queden rutas |
| `reintentable_inmediato` | Permitido, con tope de frecuencia | Puede reintentarse por ruta alterna |
| `reintentable_diferido` | Solo tras ventana larga | La intención se rechaza; el reintento es una intención nueva |

**El tope de frecuencia se cuenta por instrumento y por periodo, no por intención.** Un reintento que
respeta el límite dentro de una intención pero lo excede sumando intenciones previas está en
incumplimiento.

Decisión provisional **N-5**. Invariante I18.

> **Por qué es del canónico y no del cliente.** El exceso de reintentos tiene consecuencia ante marca,
> y en el modelo de cascada esa consecuencia recae sobre la afiliación completa. Un integrador que
> reintenta mal quema a todos los sub-afiliados que cuelgan de la misma afiliación.

---

### 3.4 Autenticación · `authentication`

| Estado | Significado |
|---|---|
| `no_aplica` | Canal o producto que no requiere autenticación |
| `autenticado` | Autenticación plena |
| `intento_registrado` | Se intentó autenticar y quedó registro |
| `no_autenticado` | Sin autenticación |
| `fallida` | La autenticación se intentó y falló |

**De este dato depende el traslado de responsabilidad en un contracargo.** Se persiste siempre y en
términos canónicos. Cada marca codifica estos estados con valores distintos y cada cámara los
transporta en campos distintos: **ambas cosas viven en la tabla de mapeo, nunca aquí.**

> Los estados y su codificación por marca **ya están documentados en cámara**, así que este apartado
> deja de depender de acceso patrocinado a documentación de marca. Lo que sigue dependiendo de ello son
> los motivos de contracargo.

---

### 3.5 Decisión de ruteo · `routing_decision`

Objeto persistido, no cálculo efímero. Registra:

- Afiliación elegida y regla que la eligió
- **Escenario de agregación vigente de esa afiliación** al momento de decidir
- Ruta pretendida y si es preferencia o **restricción**
- Adquirente patrocinador bajo el que se emitirá
- Insumos evaluados: rango de BIN, giro, importe, marca, canal, salud de ruta
- Versión de configuración aplicada (D21)

**Se persiste aunque Centrix no pueda ejecutarla.** En Fase 0 el procesador rutea con su propio
criterio; registrar la decisión permite medir después la desviación entre lo pedido y lo hecho, que es
argumento comercial en Fase 1.

Una restricción no se degrada ante caída de ruta: si la ruta forzada no está disponible, la intención
se rechaza, no se reencamina.

---

## 4. Jerarquía comercial

### 4.1 Afiliación · `affiliation`

**Estado declarado**

| Estado | Significado |
|---|---|
| `en_alta` | Solicitada, sin confirmar |
| `activa` | Operando |
| `suspendida` | Reversible; no se envía |
| `cancelada` | Terminal; arrastra a todas sus sub-afiliaciones |

**Estado observado** (P3)

| Estado | Significado |
|---|---|
| `operando` | Respuestas normales |
| `rechazando_estructural` | N rechazos estructurales consecutivos; cortacircuitos abierto |
| `sin_respuesta` | Ruta caída |
| `desconocido` | Sin tráfico suficiente para inferir |

**La divergencia entre declarado y observado es la señal de alerta.** Centrix se entera del cierre de
una afiliación de forma reactiva, por los rechazos: la cámara ya lo sabe y declina todo. Por eso el
observado no es cosmético — es el mecanismo de detección.

**Atributos con vigencia.** La afiliación lleva su **escenario de agregación** (0.1 §2.2) y su
**adquirente patrocinador** como atributos versionados, no como constantes. Cambiar de escenario cambia
la tarifa que paga cada sub-afiliado y qué datos deben viajar en cada mensaje: es un hecho con fecha,
no una corrección.

Decisión provisional **N-3**.

---

### 4.2 Identificador de facilitador de pagos · `payment_facilitator_id`

**Entidad nueva en esta versión.** Cuelga de la afiliación, uno por marca.

| Estado | Significado |
|---|---|
| `no_tramitado` | La afiliación no tiene identificador para esa marca |
| `en_tramite` | Solicitado a la marca, sin asignar |
| `vigente` | Asignado y utilizable |
| `revocado` | Terminal; la marca lo retiró |

**Transiciones**

| Origen | Evento | Destino |
|---|---|---|
| `no_tramitado` | solicitud registrada | `en_tramite` |
| `en_tramite` | asignación por la marca | `vigente` |
| `en_tramite` | rechazo o vencimiento | `no_tramitado` |
| `vigente` | revocación por la marca | `revocado` |

**Centrix refleja, no decide.** El identificador lo otorga la marca y lo tramita el titular de
afiliación por su propio canal. Centrix lo registra, verifica su presencia antes de emitir cuando el
escenario lo exige, y **rechaza estructuralmente** si falta donde es obligatorio — sin intentar
suplirlo con ningún otro identificador.

Decisión provisional **N-4**.

> **Por qué no es un campo de la afiliación.** Tiene emisor distinto (la marca, no el adquirente),
> cardinalidad distinta (uno por marca) y ciclo de vida propio, incluida la revocación unilateral.
> Modelarlo como columna obliga a una columna nueva por cada marca que se incorpore.

---

### 4.3 Sub-afiliación · `sub_affiliation`

| Estado | Significado |
|---|---|
| `en_alta` | Solicitada al procesador, sin confirmar |
| `activa` | Operando |
| `suspendida` | Reversible; decisión del titular de afiliación |
| `cierre_solicitado` | Instrucción emitida al tercero, sin confirmar (P4) |
| `cerrada` | Terminal |

**Transiciones**

| Origen | Evento | Destino |
|---|---|---|
| `en_alta` | confirmación del procesador | `activa` |
| `en_alta` | rechazo o vencimiento | `cerrada` |
| `activa` | suspensión por el titular | `suspendida` |
| `suspendida` | reactivación | `activa` |
| `activa` o `suspendida` | cierre iniciado en el portal | `cierre_solicitado` |
| `cierre_solicitado` | confirmación del tercero | `cerrada` |
| `cierre_solicitado` | vencimiento sin confirmar | `cierre_solicitado` + alerta |
| cualquiera | cascada por cancelación de afiliación | `cerrada` |

**Origen del identificador según el escenario.** En escenario `registrado`, el identificador proviene
del padrón de la cámara y el alta pasa por ella. En escenario `no_registrado`, **lo asigna el propio
titular de afiliación bajo su responsabilidad de unicidad**, y con él debe viajar el expediente mínimo
del sub-afiliado en cada mensaje: giro, domicilio, código postal, ciudad, estado y país.

Consecuencia de modelo: en `no_registrado` el expediente del sub-afiliado **no es dato de referencia,
es carga útil de cada transacción**, y su calidad determina si el mensaje se acepta.

**Suspensión y cierre no son lo mismo.** La suspensión es reversible y es la herramienta del titular
de afiliación ante sospecha de fraude; el cierre es terminal.

> **Por qué existe el cierre preventivo.** El titular de afiliación cierra una sub-afiliación para
> evitar que las marcas le cierren la afiliación entera. La cascada existe precisamente porque el
> eslabón de arriba responde por los de abajo — y esa es la razón de negocio por la que el titular
> hace onboarding y vigila giros.

---

### 4.4 Cascada de cancelación

Cancelar una afiliación cierra **todas** sus sub-afiliaciones, sin excepción.

**No es una transacción de base de datos.** Cientos de comercios bajo una afiliación hacen de esto un
proceso con:

- Progreso observable y reanudable
- Bitácora por sub-afiliación con el motivo propagado
- Reintento de las instrucciones no confirmadas
- Invariante final: ninguna sub-afiliación activa bajo afiliación cancelada (I11)

Mientras la cascada corre, la afiliación ya no envía. La consistencia es eventual hacia abajo, pero el
corte de tráfico es inmediato.

**Dinero en vuelo.** No lo resuelve este documento. El modelo lo absorbe —lo conciliado y confirmado
es deuda cierta, lo indeterminado no se dispersa (I4, I6)— pero **quién decide, con qué autorización y
en qué plazo es procedimiento operativo**, y vive en el documento 6.6.

---

### 4.5 Terminal · `terminal`

Identidad dual: identificador interno y de red (D45).

**Estado de ciclo de vida**

| Estado | Significado |
|---|---|
| `registrada` | Existe en Centrix, sin identificador de red |
| `alta_solicitada` | Solicitada a la red, sin confirmar (P4) |
| `activa` | Con identificador de red asignado y operando |
| `suspendida` | Reversible |
| `baja` | Terminal |

El alta no es una escritura local. Y como el identificador de red lo asigna la cámara, **el plazo de
esa asignación entra en el procedimiento de incorporación de cliente**.

**Estado criptográfico** — eje independiente del anterior

| Estado | Significado |
|---|---|
| `sin_inicializar` | Nunca recibió llaves de trabajo |
| `inicializada` | Llaves cargadas y utilizables |
| `renovacion_requerida` | La cámara señaló que debe reinicializar |
| `agotada` | Alcanzó el tope de uso de la carga vigente |
| `fallo_criptografico` | Fallos consecutivos; no debe operar |

**Transiciones**

| Origen | Evento | Destino |
|---|---|---|
| `sin_inicializar` | inicialización confirmada | `inicializada` |
| `inicializada` | indicación de renovación de la cámara | `renovacion_requerida` |
| `inicializada` | tope de uso alcanzado | `agotada` |
| `inicializada` | fallos criptográficos consecutivos | `fallo_criptografico` |
| `renovacion_requerida` o `agotada` | reinicialización confirmada | `inicializada` |
| `fallo_criptografico` | reinicialización confirmada | `inicializada` |

**Una terminal `sin_inicializar` no puede emitir.** No es una degradación de servicio: es que sin
llaves cargadas el contenido de tarjeta se transmite sin proteger, y eso arrastra a Centrix al alcance
máximo de cumplimiento. El rechazo es **estructural** y no configurable. Invariante I16.

El detalle del intercambio de llaves —secuencia, códigos de resultado, contadores y método de
protección— vive en el documento **0.11**. Aquí solo viven los estados y su efecto sobre la emisión.

Decisión provisional **N-7**.

---

## 5. Ciclo del dinero

### 5.1 Lote · `batch`

| Estado | Transiciones |
|---|---|
| `abierto` | → `cierre_solicitado` |
| `cierre_solicitado` | → `cerrado` (confirmación) · → `abierto` + alerta (fallo) |
| `cerrado` | → `compensado` |
| `compensado` | terminal |

Define la frontera del día operativo y por tanto la unidad de conciliación. **Sin cierre no hay
compensación y no hay conciliación posible.**

Una transacción cerca de medianoche puede caer en cortes distintos si los relojes no están
sincronizados: de ahí el requisito de sincronización horaria.

> **No todas las cámaras cierran lote por mensaje.** Al menos una de las dos no define un tipo de
> mensaje de cierre en su estándar de punto de venta: la compensación pasa por otra vía. El canónico
> conserva el lote como concepto de negocio; **cómo se cierra es capacidad del riel**.

---

### 5.1b Captura diferida y fichero de compensación

**Apartado nuevo.** Toda captura se emite bajo uno de dos modos, declarado en el mensaje:

| Modo | Qué ocurre |
|---|---|
| `integrada` | La transacción se autoriza y compensa de forma secuencial |
| `diferida` | La autorización se separa de la compensación; **la compensación exige un fichero posterior** |

En modo `diferida`, **si el fichero no se envía, la transacción queda autorizada y nunca se liquida**.
No hay rechazo, no hay alerta de la cámara, no hay error: simplemente el dinero no se mueve.

**Estados del fichero de compensación** · `clearing_file`

| Estado | Significado |
|---|---|
| `en_preparacion` | Acumulando transacciones de captura diferida |
| `emitido` | Entregado al tercero, sin confirmar |
| `confirmado` | El tercero acusó recepción y aceptación |
| `rechazado` | El tercero lo rechazó; las transacciones vuelven a `en_preparacion` |
| `parcialmente_aceptado` | Parte de las partidas aceptadas; el resto vuelve |

**Ninguna captura en modo diferido se considera compensada mientras su fichero no esté `confirmado`.**
Invariante I17.

Decisión provisional **N-6**.

> **Por qué es invariante y no proceso.** Es la única pérdida del modelo que no produce ninguna señal
> de error. Un asiento que se marca compensado sin confirmación de fichero es un asiento que afirma un
> hecho que no ocurrió, y el descuadre aparece semanas después, en la conciliación bancaria.

---

### 5.2 Asiento contable · `ledger_entry`

Partida doble, inmutable, con **estado dual** (P3):

| Estado | Significado |
|---|---|
| `aprobado` | Lo que Centrix cree según su propio registro |
| `confirmado` | Lo que el tercero verificó en conciliación |

**Corrección por contrapartida, nunca por edición.** Un libro editable no es evidencia contable ni de
auditoría.

Eventos que generan asiento: autorización, captura, anulación, devolución, comisión, liquidación,
dispersión, contracargo y su resolución, ajuste de conciliación. **Autorización y captura generan
asientos distintos** — es la razón contable por la que no se pueden colapsar, y ahora también la razón
mecánica: en modo diferido ocurren en momentos distintos y por vías distintas.

---

### 5.3 Liquidación y dispersión

`settlement` calcula cuánto corresponde a cada parte tras comisiones, retenciones y ajustes.
`payout` mueve el dinero.

| Estado de dispersión | Significado |
|---|---|
| `calculada` | Importe determinado |
| `bloqueada` | Existe partida no conciliada, transacción indeterminada o fichero sin confirmar |
| `instruida` | Archivo o instrucción emitida |
| `confirmada` | Confirmada por el banco |
| `fallida` | Rechazada; requiere gestión |

**Invariante: no se dispersa lo no conciliado (I6), ni lo indeterminado (I4), ni lo no compensado
(I17).** El bloqueo es un estado del modelo, no una regla del proceso.

---

### 5.4 Conciliación

`reconciliation_movement` es el hecho reportado por un tercero, con independencia del formato.
`reconciliation_item` es el resultado del cotejo:

| Estado | Tratamiento |
|---|---|
| `casada` | Confirma el asiento; libera la dispersión |
| `diferencia_importe` | Bloquea; requiere gestión |
| `faltante_en_origen` | El tercero reporta algo que Centrix no tiene |
| `faltante_en_destino` | Centrix tiene algo que el tercero no reporta |
| `duplicada` | Candidata a reverso no procesado |
| `resuelta` | Gestionada, con motivo y responsable |

La **llave de correlación** se define en el canónico —no en el dialecto— para que el motor exista antes
de saber cómo entrega los datos el tercero.

**Granularidad conciliable.** Queda resuelta: ambas cámaras transportan el identificador de
sub-afiliación y el de terminal, de modo que **la conciliación a nivel comercio es alcanzable**. La
sucursal sigue siendo atribución interna no conciliada, porque no viaja.

**Los identificadores propios pueden viajar.** Ambas cámaras reservan estructuras para los
identificadores de la pasarela, con jerarquía de pedido, operación y devolución. Cuando el riel lo
admite, la llave de correlación viaja en el mensaje en vez de reconstruirse; cuando no, se reconstruye.
Es capacidad del riel, y el motor de conciliación debe funcionar en ambos casos.

---

## 6. Invariantes

No son validaciones: son condiciones que el sistema no puede violar en ningún estado. Cada una tiene
control automatizado.

| # | Invariante |
|---|---|
| I1 | Todo importe es entero en unidades mínimas y lleva moneda con exponente explícito |
| I2 | La suma de capturas de una intención nunca excede el importe autorizado |
| I3 | La suma de devoluciones nunca excede el importe capturado |
| I4 | Nada en estado indeterminado se liquida ni se dispersa |
| I5 | Todo asiento cuadra en partida doble; el libro cuadra por periodo |
| I6 | No se dispersa lo no conciliado |
| I7 | Una intención tiene a lo más un intento aprobado |
| I8 | No se emite intento sobre afiliación observada como rechazando estructural |
| I9 | Toda transacción registra la versión de configuración aplicada |
| I10 | Los asientos son inmutables; la corrección es por contrapartida |
| I11 | Ninguna sub-afiliación activa bajo afiliación cancelada |
| I12 | El canónico no contiene número de cuenta principal ni código de seguridad |
| I13 | El reverso se empareja por identificadores de red, no por identificador propio |
| I14 | Toda instrucción a un tercero que cambia estado pasa por estado intermedio confirmable |
| I15 | El modelo admite N eslabones de comisión; ningún cálculo asume tres |
| **I16** | **Ninguna terminal en estado criptográfico `sin_inicializar` emite un intento** |
| **I17** | **Ninguna captura en modo diferido se marca compensada sin fichero `confirmado`** |
| **I18** | **Todo resultado declinado lleva categoría de rechazo; ningún reintento se emite sobre categoría `definitivo`** |

---

## 7. Lo que este documento deja abierto

Las preguntas 2 y 4 de la versión anterior **quedan cerradas**: la ventana de reverso está publicada
por cámara, y la granularidad conciliable llega a nivel comercio.

Quedan abiertas:

1. **Ventana de autorización antes de expirar**, por marca y producto. Requiere documentación de
   marca. Afecta 3.1.
2. **Causa de rechazo ante afiliación cerrada** y umbral del cortacircuitos. Afecta 4.1 e I8.
3. **Plazo de asignación del identificador de terminal.** Afecta 4.5 y el onboarding.
4. **Procedimiento de dinero en vuelo ante cancelación de afiliación.** No bloquea el modelo; vive en
   el documento 6.6.
5. **Tope exacto de frecuencia de reintento** por categoría y por cámara. El enumerado de 3.3 es
   estable; los números viven en la tabla de mapeo y difieren entre cámaras.
6. **¿El contenido de tarjeta viaja protegido en el mensaje de aviso?** Si no lo hiciera, I12 se
   sostiene en el canónico pero el perímetro del adaptador cambia. Registrada como Q-1.
7. **Enumerado definitivo de escenarios de agregación**, pendiente del contrato marco. Afecta 4.1.

---

*Centrix · Capa 0 · Documento 0.2 · Interno confidencial*
